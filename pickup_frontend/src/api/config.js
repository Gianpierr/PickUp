// src/api/config.js

/**
 * Centralized configuration for backend API base URL.
 * Change this value when switching between development, staging, and production.
 */
export const BASE_URL = "http://127.0.0.1:8000";